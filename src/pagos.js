/**
 * pagos.js - Punto de entrada para pagos.html
 *
 * Orquesta la inicialización de la página de estados de pago.
 */

import { initPagosPage } from './ui/pages/pagos.js';

document.addEventListener('DOMContentLoaded', () => {
    initPagosPage();
});