/**
 * ordenes.js - Punto de entrada para ordenes.html
 *
 * Orquesta la inicialización de la página de órdenes de trabajo.
 */

import { initOrdenesPage } from './ui/pages/ordenes.js';

document.addEventListener('DOMContentLoaded', () => {
    initOrdenesPage();
});