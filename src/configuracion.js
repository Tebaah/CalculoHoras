/**
 * configuracion.js - Punto de entrada para configuracion.html
 *
 * Orquesta la inicialización de la página de configuración.
 */

import { initConfiguracionPage } from './ui/pages/configuracion.js';

document.addEventListener('DOMContentLoaded', () => {
    initConfiguracionPage();
});
